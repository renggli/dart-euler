import 'dart:async';

import '../problem.dart';
import '../runner.dart';

class Euler extends Problem {
  Euler({required int number}) : super(label: 'Euler $number');

  @override
  FutureOr<void> run(Runner runner) {
    throw UnimplementedError();
  }
}
